# Evaluators

Phase 3 will contain deterministic rules, classifiers and optional LLM judges. Evaluators produce normalized evidence and confidence rather than directly owning provider-specific output parsing.
